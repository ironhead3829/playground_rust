# Added by Cargo: contains compiled files, executables, and build artifacts
/target/

# Log files generated by Cargo (if any)
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Optional: Cargo.lock
# Keep Cargo.lock if you are building an application/executable.
# Remove or uncomment it if you are building a library.
# Cargo.lock
